// Q2   2/9
// -1 missing class header

// -1 missing fields/properties
public CombinedTable(SingleTable t1, SingleTable t2) {
    this.table1 = t1;
    this.table2 = t2;
}

// +1 correct method header
public boolean canSeat(int sumPeople){
    int totalSeat = int getNumSeats.table1 + int getNumSeats.table2;
    int canSeat = int totalSeat - 2;
}

// +1 correct method header
public double getDesirability(){
    if (table1.getHeight() == table2.getHeight()){
        return (table1.getHeight() + table2.getHeight()) / 2;
    } else {
        return (table1.getHeight() + table2.getHeight()) / 2 - 10.0;
    }
    public int setViewQuality(int numQuality){

    }
