public class Rand {
    public static void main(String[] args) {
        double randomNum = Math.random() * 100;
        // 0 <= randomNum < 1
        // if we * a number x, than the range of the number will be increased to 0 - x;
        // if we + a number y, than the starting value and the end value will be y - y+x;
    }
}
